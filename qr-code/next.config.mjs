// qr-code/next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  distDir: 'dist/qr-code',
  basePath: '/frontend-practice', // Ensure this is set correctly if you are using a base path
};

export default nextConfig;
